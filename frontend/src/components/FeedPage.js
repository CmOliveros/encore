import React, {Component} from "react";

export default class FeedPage extends Component {

    constructor(props) {
        super(props);
    }

    render() {
        return <p>This is the feed page</p>
    }
}